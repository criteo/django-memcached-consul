language: python

dist: trusty

matrix:
  include:
    - python: 'pypy3'
      env: TOXENV=pypy3
    - python: '3.6'
      env: TOXENV=py36
    - python: '3.6'
      env: TOXENV=pylama

install:
  - pip install tox coveralls

script:
  - tox

sudo: required

after_success:
  - coveralls
